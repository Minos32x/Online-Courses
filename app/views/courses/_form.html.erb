<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <div class="text-danger"> <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2></div>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><div class="text-danger" style="font-size:15px"> <%= message  %></div> </li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "title:" , :style=>'font-size:25px' %>
    <%= form.text_field :title, id: :course_title , :class => 'form-control' %> 
  </div>

  <div class="field">
  
    <%= form.text_field :user_id, id: :user_id  ,value:current_user.id,hidden:true %>
  </div>
<br>
  <div class="actions">
    <%= form.submit :class => 'btn btn-success' ,:style=>'font-size:15px' %>
  </div>
<% end %>
